---
import Layout from '../../../layouts/Layout.astro';
import { useTranslations } from '../../../i18n/utils';

const t = useTranslations('es');
---

<Layout title="Synaptic Digest - arsomnia" lang="es">
	<!-- Hero Section -->
	<section class="project-hero">
		<div class="container">
			<div class="hero-content">
				<a href="/es/#proyectos" class="back-link">← Volver a Proyectos</a>
				<h1 class="project-title">Synaptic Digest</h1>
				<p class="project-tagline">Inteligencia de investigación impulsada por IA que monitorea 10.000+ papers mensuales</p>
			</div>
		</div>
	</section>

	<!-- Hero Image -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-hero">
				<img src="/email-hero-synaptic.png" alt="Email de resumen de investigación mensual" />
				<p class="image-caption">Resumen de investigación mensual entregado automáticamente con insights curados por IA de la literatura en ciencias cognitivas</p>
			</div>
		</div>
	</section>

	<!-- The Problem -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">El Problema</h2>
			<p class="section-text">
				Los profesionales de la investigación necesitan mantenerse al día con cientos de artículos publicados mensualmente, pero la revisión manual consume tiempo y a menudo pierde patrones emergentes.
			</p>
			<p class="section-text">
				Los académicos pasan más tiempo buscando artículos relevantes que analizando sus implicaciones.
			</p>
		</div>
	</section>

	<!-- The Solution -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">La Solución</h2>
			<p class="section-text">
				Un pipeline autónomo que monitorea bases de datos académicas, analiza hallazgos con IA y entrega insights estructurados mensualmente—sin ninguna intervención manual.
			</p>
		</div>
	</section>

	<!-- How It Works -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Cómo Funciona</h2>
			
			<div class="workflow-step">
				<h3 class="step-title">1. Monitoreo Programado</h3>
				<p class="section-text">
					El día 1 de cada mes, el sistema consulta automáticamente OpenAlex para artículos publicados en los últimos 30 días, filtrando por ciencias cognitivas y temas de investigación cerebral.
				</p>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">2. Transformación de Datos</h3>
				<p class="section-text">
					Un módulo JavaScript extrae metadatos (autores, citas, temas, DOIs) y los formatea en texto limpio y legible para el análisis de IA.
				</p>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">3. Análisis IA</h3>
				<p class="section-text">Claude IA procesa el corpus de investigación, identificando:</p>
				<ul class="feature-list">
					<li>Hallazgos clave y avances</li>
					<li>Tendencias emergentes entre estudios</li>
					<li>Implicaciones prácticas para el campo</li>
					<li>Artículos más significativos con citas</li>
				</ul>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">4. Formato y Entrega</h3>
				<p class="section-text">
					El markdown generado por IA se convierte en HTML responsive y se envía vía Gmail con una plantilla profesional con encabezados en degradado y secciones estructuradas.
				</p>
			</div>
		</div>
	</section>

	<!-- Workflow Screenshot -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-workflow">
				<img src="/n8n-workflow-synaptic.png" alt="Workflow n8n" />
				<p class="image-caption">El workflow n8n orquestando llamadas API, transformaciones de datos y entrega de emails</p>
			</div>
		</div>
	</section>

	<!-- Stack -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Stack</h2>
			<div class="stack-table">
				<div class="stack-row">
					<div class="stack-tool">n8n</div>
					<div class="stack-role">Orquestación de workflows</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">OpenAlex API</div>
					<div class="stack-role">Base de datos académica (10M+ artículos)</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">Anthropic Claude</div>
					<div class="stack-role">Análisis y síntesis IA</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">Gmail API</div>
					<div class="stack-role">Entrega de emails</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">JavaScript</div>
					<div class="stack-role">Transformación de datos y conversión HTML</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Results -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Resultados</h2>
			<div class="results-grid">
				<div class="result-item">
					<div class="result-number">10.000+</div>
					<div class="result-text">artículos monitoreados mensualmente</div>
				</div>
				<div class="result-item">
					<div class="result-number">4h</div>
					<div class="result-text">ahorradas por mes en investigación manual</div>
				</div>
				<div class="result-item">
					<div class="result-number">100%</div>
					<div class="result-text">tasa de automatización (cero intervención)</div>
				</div>
				<div class="result-item">
					<div class="result-number">$0.20</div>
					<div class="result-text">costo operativo mensual</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Email Detail -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-centered">
				<img src="/email-detail-synaptic.png" alt="Detalle del email" />
				<p class="image-caption">Insights generados por IA estructurados en secciones: Hallazgos Clave, Tendencias Emergentes, Implicaciones Prácticas y Referencias</p>
			</div>
		</div>
	</section>

	<!-- CTA Section -->
	<section class="project-section project-cta">
		<div class="container-narrow">
			<div class="cta-buttons">
				<a href="/es/#contacto" class="btn btn-primary">Contactar</a>
				<a href="/es/#proyectos" class="btn btn-secondary">Ver Más Proyectos</a>
			</div>
		</div>
	</section>

	<!-- Footer -->
	<footer class="footer">
		<div class="container">
			<div class="footer-content">
				<p class="footer-text">© 2025 arsomnia</p>
			</div>
		</div>
	</footer>
</Layout>

<style>
	/* Project Hero */
	.project-hero {
		padding: 3rem 0 3rem;
		background: var(--color-bg);
	}

	.back-link {
		display: inline-block;
		color: var(--color-text-light);
		text-decoration: none;
		font-size: 0.9rem;
		margin-bottom: 3rem;
		transition: all 0.3s ease;
		opacity: 0.7;
		letter-spacing: 0.02em;
	}

	.back-link:hover {
		color: var(--color-accent);
		opacity: 1;
		transform: translateX(-4px);
	}

	.hero-content {
		text-align: center;
		max-width: 900px;
		margin: 0 auto;
	}

	.project-title {
		font-size: 3.5rem;
		font-weight: 700;
		margin-bottom: 1rem;
		color: var(--color-text);
	}

	.project-tagline {
		font-size: 1.3rem;
		color: var(--color-text-light);
		font-weight: 300;
	}

	/* Sections */
	.project-section {
		padding: 4rem 0;
	}

	.project-cta {
		padding: 6rem 0;
	}

	.container-narrow {
		max-width: 800px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	.section-title {
		font-size: 2.5rem;
		font-weight: 700;
		margin-bottom: 2rem;
		color: var(--color-text);
	}

	.section-text {
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--color-text-light);
		margin-bottom: 1.5rem;
	}

	/* Images */
	.project-image-hero {
		max-width: 950px;
		margin: 0 auto;
	}

	.project-image-workflow {
		max-width: 1000px;
		margin: 0 auto;
	}

	.project-image-centered {
		max-width: 900px;
		margin: 0 auto;
	}

	.project-image-hero img,
	.project-image-workflow img,
	.project-image-centered img {
		width: 100%;
		height: auto;
		display: block;
		border: 1px solid rgba(212, 175, 55, 0.2);
	}

	.image-caption {
		text-align: center;
		font-size: 0.9rem;
		color: var(--color-text-light);
		margin-top: 1rem;
		font-style: italic;
	}

	/* Workflow Steps */
	.workflow-step {
		margin-bottom: 3rem;
	}

	.step-title {
		font-size: 1.5rem;
		font-weight: 600;
		color: var(--color-accent);
		margin-bottom: 1rem;
	}

	.feature-list {
		list-style: none;
		padding: 0;
		margin: 1.5rem 0;
	}

	.feature-list li {
		padding-left: 1.5rem;
		position: relative;
		margin-bottom: 0.75rem;
		color: var(--color-text-light);
		font-size: 1.1rem;
	}

	.feature-list li::before {
		content: '→';
		position: absolute;
		left: 0;
		color: var(--color-accent);
	}

	/* Stack Table */
	.stack-table {
		border: 1px solid rgba(212, 175, 55, 0.2);
	}

	.stack-row {
		display: grid;
		grid-template-columns: 200px 1fr;
		border-bottom: 1px solid rgba(212, 175, 55, 0.1);
	}

	.stack-row:last-child {
		border-bottom: none;
	}

	.stack-tool {
		padding: 1.25rem 1.5rem;
		font-weight: 600;
		color: var(--color-accent);
		border-right: 1px solid rgba(212, 175, 55, 0.1);
	}

	.stack-role {
		padding: 1.25rem 1.5rem;
		color: var(--color-text-light);
	}

	/* Results Grid */
	.results-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 2rem;
		margin-top: 2rem;
	}

	.result-item {
		text-align: center;
		padding: 2rem;
		border: 1px solid rgba(212, 175, 55, 0.2);
		background: rgba(13, 13, 13, 0.3);
	}

	.result-number {
		font-size: 3rem;
		font-weight: 700;
		color: var(--color-accent);
		margin-bottom: 0.5rem;
	}

	.result-text {
		font-size: 1rem;
		color: var(--color-text-light);
	}

	/* CTA Buttons */
	.cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.btn {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 1rem 2rem;
		font-size: 1rem;
		font-weight: 500;
		text-decoration: none;
		border-radius: 0;
		transition: all 0.3s ease;
		font-family: var(--font-main);
		border: 2px solid;
	}

	.btn-primary {
		background: var(--color-accent);
		color: var(--color-bg);
		border-color: var(--color-accent);
	}

	.btn-primary:hover {
		background: transparent;
		color: var(--color-accent);
	}

	.btn-secondary {
		background: transparent;
		color: var(--color-text);
		border-color: rgba(212, 175, 55, 0.3);
	}

	.btn-secondary:hover {
		border-color: var(--color-accent);
		color: var(--color-accent);
	}

	/* Footer */
	.footer {
		padding: 3rem 0;
		border-top: 1px solid rgba(212, 175, 55, 0.2);
	}

	.footer-content {
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.footer-text {
		color: var(--color-text-light);
		font-size: 0.95rem;
	}

	/* Responsive */
	@media (max-width: 768px) {
		.project-hero {
			padding: 2.5rem 0 2rem;
		}

		.project-hero .container,
		.project-section .container,
		.project-section .container-narrow {
			padding-left: 1.5rem;
			padding-right: 1.5rem;
		}

		.back-link {
			font-size: 0.85rem;
			margin-bottom: 2.5rem;
		}

		.project-title {
			font-size: 2.5rem;
		}

		.project-tagline {
			font-size: 1.1rem;
		}

		.section-title {
			font-size: 2rem;
		}

		.section-text {
			font-size: 1rem;
		}

		.stack-row {
			grid-template-columns: 1fr;
		}

		.stack-tool {
			border-right: none;
			border-bottom: 1px solid rgba(212, 175, 55, 0.1);
		}

		.results-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.result-number {
			font-size: 2.5rem;
		}

		.cta-buttons {
			flex-direction: column;
		}

		.btn {
			width: 100%;
			justify-content: center;
		}
	}
</style>
