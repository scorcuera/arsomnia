---
import Layout from '../../../layouts/Layout.astro';
import { useTranslations } from '../../../i18n/utils';

const t = useTranslations('fr');
---

<Layout title="Énergie Sync - arsomnia" lang="fr">
	<!-- Hero Section -->
	<section class="project-hero">
		<div class="container">
			<div class="hero-content">
				<a href="/fr/#proyectos" class="back-link">← Retour aux Projets</a>
				<h1 class="project-title">Énergie Sync</h1>
				<p class="project-tagline">Réconciliation automatisée de facturation pour fournisseurs d'énergie</p>
			</div>
		</div>
	</section>

	<!-- Hero Image -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-hero">
				<img src="/dashboard-energie-hero.png" alt="Tableau de bord des anomalies en temps réel" />
				<p class="image-caption">Tableau de bord des anomalies en temps réel pour plus de 40 clients</p>
			</div>
			<div class="dashboard-cta">
				<a href="https://airtable.com/invite/l?inviteId=invCJzK4B0nbgqYBV&inviteToken=be690f13ece97d1fa980fea17ee24a8b207fd3ee4c0677b8bd7c7c8c824d04b7&utm_medium=email&utm_source=product_team&utm_content=transactional-alerts" target="_blank" rel="noopener noreferrer" class="btn-dashboard">
					<span>Explorer le Dashboard</span>
					<svg width="16" height="16" viewBox="0 0 16 16" fill="none">
						<path d="M6 3L11 8L6 13" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
					</svg>
				</a>
			</div>
		</div>
	</section>

	<!-- The Problem -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Le Problème</h2>
			<p class="section-text">
				Les fournisseurs d'énergie gèrent les données de consommation, les factures et les paiements sur des systèmes déconnectés. 
				Réconcilier ces sources manuellement prend des heures à chaque cycle—et des erreurs passent entre les mailles.
			</p>
			<p class="section-text">
				Les équipes Finance passent plus de temps à traquer les écarts qu'à analyser les tendances.
			</p>
		</div>
	</section>

	<!-- The Solution -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">La Solution</h2>
			<p class="section-text">
				Un pipeline automatisé qui consolide trois sources de données, croise chaque transaction et fait remonter les anomalies avant qu'elles ne deviennent des problèmes.
			</p>
		</div>
	</section>

	<!-- How It Works -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Fonctionnement</h2>
			
			<div class="workflow-step">
				<h3 class="step-title">1. Ingestion des Données</h3>
				<p class="section-text">
					Les relevés de consommation, les factures et les paiements bancaires sont récupérés automatiquement depuis leurs systèmes respectifs.
				</p>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">2. Moteur de Réconciliation</h3>
				<p class="section-text">
					Un workflow n8n personnalisé fusionne les enregistrements par client et par période, puis exécute des règles de validation sur l'ensemble consolidé.
				</p>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">3. Détection d'Anomalies</h3>
				<p class="section-text">Le système identifie trois types d'écarts :</p>
				<ul class="feature-list">
					<li>Consommation sans facture (enregistrements orphelins)</li>
					<li>Écarts entre facture et paiement (différences de montant)</li>
					<li>Factures en retard sans règlement</li>
				</ul>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">4. Tableau de Bord Opérationnel</h3>
				<p class="section-text">
					Les résultats alimentent une Interface Airtable où l'équipe Opérations peut trier, assigner et résoudre chaque cas.
				</p>
			</div>
		</div>
	</section>

	<!-- Workflow Screenshot -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-workflow">
				<img src="/n8n-workflow-energie.png" alt="Workflow n8n" />
				<p class="image-caption">Le workflow n8n gérant la fusion des données et la logique de détection</p>
			</div>
			<div class="workflow-cta">
				<button class="btn-dashboard" id="openVideoBtn">
					<span>Voir le workflow en action</span>
					<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
						<polygon points="5 3 19 12 5 21 5 3"></polygon>
					</svg>
				</button>
			</div>
		</div>
	</section>

	<!-- Video Modal -->
	<div class="video-modal" id="videoModal">
		<div class="modal-overlay" id="modalOverlay"></div>
		<div class="modal-content">
			<button class="modal-close" id="closeModalBtn">
				<svg width="24" height="24" viewBox="0 0 24 24" fill="none">
					<path d="M18 6L6 18M6 6L18 18" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
				</svg>
			</button>
			<div class="video-container">
				<video id="demoVideo" controls>
					<source src="/energie_sync_demo.mp4" type="video/mp4">
					Votre navigateur ne supporte pas la balise vidéo.
				</video>
			</div>
		</div>
	</div>

	<!-- Stack -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Stack Technique</h2>
			<div class="stack-table">
				<div class="stack-row">
					<div class="stack-tool">n8n</div>
					<div class="stack-role">Orchestration & logique métier</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">Airtable</div>
					<div class="stack-role">Stockage des données & tableau de bord</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">Google Sheets</div>
					<div class="stack-role">Source des données de consommation</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">API REST</div>
					<div class="stack-role">Intégration des paiements bancaires</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Results -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Résultats</h2>
			<div class="results-grid">
				<div class="result-item">
					<div class="result-number">3</div>
					<div class="result-text">sources de données consolidées automatiquement</div>
				</div>
				<div class="result-item">
					<div class="result-number">12</div>
					<div class="result-text">anomalies détectées par cycle</div>
				</div>
				<div class="result-item">
					<div class="result-number">~2h</div>
					<div class="result-text">économisées par réconciliation</div>
				</div>
				<div class="result-item">
					<div class="result-number">0</div>
					<div class="result-text">saisie manuelle</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Dashboard Detail -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-centered">
				<img src="/dashboard-energie-detail-1.png" alt="Détail du dashboard" />
				<p class="image-caption">Répartition des anomalies par type et sévérité, avec actions recommandées</p>
			</div>
		</div>
	</section>

	<!-- CTA Section -->
	<section class="project-section project-cta">
		<div class="container-narrow">
			<div class="cta-buttons">
				<a href="/fr/#contacto" class="btn btn-primary">Me Contacter</a>
				<a href="/fr/#proyectos" class="btn btn-secondary">Voir Plus de Projets</a>
			</div>
		</div>
	</section>

	<!-- Footer -->
	<footer class="footer">
		<div class="container">
			<div class="footer-content">
				<p class="footer-text">© 2025 arsomnia</p>
			</div>
		</div>
	</footer>
</Layout>

<style>
	/* Project Hero */
	.project-hero {
		padding: 3rem 0 3rem;
		background: var(--color-bg);
	}

	.back-link {
		display: inline-block;
		color: var(--color-text-light);
		text-decoration: none;
		font-size: 0.9rem;
		margin-bottom: 3rem;
		transition: all 0.3s ease;
		opacity: 0.7;
		letter-spacing: 0.02em;
	}

	.back-link:hover {
		color: var(--color-accent);
		opacity: 1;
		transform: translateX(-4px);
	}

	.hero-content {
		text-align: center;
		max-width: 900px;
		margin: 0 auto;
	}

	.project-title {
		font-size: 3.5rem;
		font-weight: 700;
		margin-bottom: 1rem;
		color: var(--color-text);
	}

	.project-tagline {
		font-size: 1.3rem;
		color: var(--color-text-light);
		font-weight: 300;
	}

	/* Sections */
	.project-section {
		padding: 4rem 0;
	}

	.project-cta {
		padding: 6rem 0;
	}

	.container-narrow {
		max-width: 800px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	.section-title {
		font-size: 2.5rem;
		font-weight: 700;
		margin-bottom: 2rem;
		color: var(--color-text);
	}

	.section-text {
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--color-text-light);
		margin-bottom: 1.5rem;
	}

	/* Images */
	.project-image-hero {
		max-width: 900px;
		margin: 0 auto;
	}

	.project-image-workflow {
		max-width: 1000px;
		margin: 0 auto;
	}

	.project-image-centered {
		max-width: 900px;
		margin: 0 auto;
	}

	.project-image-hero img,
	.project-image-workflow img,
	.project-image-centered img {
		width: 100%;
		height: auto;
		display: block;
		border: 1px solid rgba(212, 175, 55, 0.2);
	}

	.image-caption {
		text-align: center;
		font-size: 0.9rem;
		color: var(--color-text-light);
		margin-top: 1rem;
		font-style: italic;
	}

	/* Dashboard CTA */
	.dashboard-cta {
		display: flex;
		justify-content: center;
		margin-top: 2rem;
	}

	.btn-dashboard {
		display: inline-flex;
		align-items: center;
		gap: 0.75rem;
		padding: 0.9rem 1.8rem;
		background: transparent;
		color: var(--color-accent);
		text-decoration: none;
		font-size: 0.95rem;
		font-weight: 500;
		border: 1.5px solid var(--color-accent);
		transition: all 0.3s ease;
		letter-spacing: 0.02em;
	}

	.btn-dashboard:hover {
		background: var(--color-accent);
		color: var(--color-bg);
		transform: translateY(-2px);
		box-shadow: 0 4px 12px rgba(212, 175, 55, 0.3);
	}

	.btn-dashboard svg {
		transition: transform 0.3s ease;
	}

	.btn-dashboard:hover svg {
		transform: translateX(3px);
	}

	/* Workflow Steps */
	.workflow-step {
		margin-bottom: 3rem;
	}

	.step-title {
		font-size: 1.5rem;
		font-weight: 600;
		color: var(--color-accent);
		margin-bottom: 1rem;
	}

	.feature-list {
		list-style: none;
		padding: 0;
		margin: 1.5rem 0;
	}

	.feature-list li {
		padding-left: 1.5rem;
		position: relative;
		margin-bottom: 0.75rem;
		color: var(--color-text-light);
		font-size: 1.1rem;
	}

	.feature-list li::before {
		content: '→';
		position: absolute;
		left: 0;
		color: var(--color-accent);
	}

	/* Stack Table */
	.stack-table {
		border: 1px solid rgba(212, 175, 55, 0.2);
	}

	.stack-row {
		display: grid;
		grid-template-columns: 200px 1fr;
		border-bottom: 1px solid rgba(212, 175, 55, 0.1);
	}

	.stack-row:last-child {
		border-bottom: none;
	}

	.stack-tool {
		padding: 1.25rem 1.5rem;
		font-weight: 600;
		color: var(--color-accent);
		border-right: 1px solid rgba(212, 175, 55, 0.1);
	}

	.stack-role {
		padding: 1.25rem 1.5rem;
		color: var(--color-text-light);
	}

	/* Results Grid */
	.results-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 2rem;
		margin-top: 2rem;
	}

	.result-item {
		text-align: center;
		padding: 2rem;
		border: 1px solid rgba(212, 175, 55, 0.2);
		background: rgba(13, 13, 13, 0.3);
	}

	.result-number {
		font-size: 3rem;
		font-weight: 700;
		color: var(--color-accent);
		margin-bottom: 0.5rem;
	}

	.result-text {
		font-size: 1rem;
		color: var(--color-text-light);
	}

	/* CTA Buttons */
	.cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.btn {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 1rem 2rem;
		font-size: 1rem;
		font-weight: 500;
		text-decoration: none;
		border-radius: 0;
		transition: all 0.3s ease;
		font-family: var(--font-main);
		border: 2px solid;
	}

	.btn-primary {
		background: var(--color-accent);
		color: var(--color-bg);
		border-color: var(--color-accent);
	}

	.btn-primary:hover {
		background: transparent;
		color: var(--color-accent);
	}

	.btn-secondary {
		background: transparent;
		color: var(--color-text);
		border-color: rgba(212, 175, 55, 0.3);
	}

	.btn-secondary:hover {
		border-color: var(--color-accent);
		color: var(--color-accent);
	}

	/* Footer */
	.footer {
		padding: 3rem 0;
		border-top: 1px solid rgba(212, 175, 55, 0.2);
	}

	.footer-content {
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.footer-text {
		color: var(--color-text-light);
		font-size: 0.95rem;
	}

	/* Responsive */
	@media (max-width: 768px) {
		.project-hero {
			padding: 2.5rem 0 2rem;
		}

		.project-hero .container,
		.project-section .container,
		.project-section .container-narrow {
			padding-left: 1.5rem;
			padding-right: 1.5rem;
		}

		.back-link {
			font-size: 0.85rem;
			margin-bottom: 2.5rem;
		}

		.project-title {
			font-size: 2.5rem;
		}

		.project-tagline {
			font-size: 1.1rem;
		}

		.section-title {
			font-size: 2rem;
		}

		.section-text {
			font-size: 1rem;
		}

		.stack-row {
			grid-template-columns: 1fr;
		}

		.stack-tool {
			border-right: none;
			border-bottom: 1px solid rgba(212, 175, 55, 0.1);
		}

		.results-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.result-number {
			font-size: 2.5rem;
		}

		.cta-buttons {
			flex-direction: column;
		}

		.btn {
			width: 100%;
			justify-content: center;
		}
	}

	/* Workflow Video CTA */
	.workflow-cta {
		display: flex;
		justify-content: center;
		margin-top: 2rem;
	}

	/* Video Modal */
	.video-modal {
		display: none;
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: 9999;
		align-items: center;
		justify-content: center;
	}

	.video-modal.active {
		display: flex;
	}

	.modal-overlay {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: rgba(0, 0, 0, 0.92);
		backdrop-filter: blur(4px);
	}

	.modal-content {
		position: relative;
		width: 90%;
		max-width: 1200px;
		z-index: 10000;
		animation: modalFadeIn 0.3s ease;
	}

	@keyframes modalFadeIn {
		from {
			opacity: 0;
			transform: scale(0.95);
		}
		to {
			opacity: 1;
			transform: scale(1);
		}
	}

	.modal-close {
		position: absolute;
		top: -3rem;
		right: 0;
		background: none;
		border: none;
		color: var(--color-text-light);
		cursor: pointer;
		padding: 0.5rem;
		transition: all 0.3s ease;
		z-index: 10001;
	}

	.modal-close:hover {
		color: var(--color-accent);
		transform: rotate(90deg);
	}

	.video-container {
		position: relative;
		width: 100%;
		background: #000;
		border: 1px solid rgba(212, 175, 55, 0.3);
	}

	.video-container video {
		width: 100%;
		height: auto;
		display: block;
	}

	/* Responsive Modal */
	@media (max-width: 768px) {
		.modal-content {
			width: 95%;
		}

		.modal-close {
			top: -2.5rem;
		}
	}
</style>

<script>
	document.addEventListener('DOMContentLoaded', () => {
		const modal = document.getElementById('videoModal');
		const openBtn = document.getElementById('openVideoBtn');
		const closeBtn = document.getElementById('closeModalBtn');
		const overlay = document.getElementById('modalOverlay');
		const video = document.getElementById('demoVideo') as HTMLVideoElement;

		// Open modal
		openBtn?.addEventListener('click', () => {
			modal?.classList.add('active');
			document.body.style.overflow = 'hidden';
			// Lazy load: start loading video when modal opens
			if (video && !video.src) {
				video.load();
			}
		});

		// Close modal function
		const closeModal = () => {
			modal?.classList.remove('active');
			document.body.style.overflow = '';
			// Pause video when closing
			if (video) {
				video.pause();
			}
		};

		// Close on button click
		closeBtn?.addEventListener('click', closeModal);

		// Close on overlay click
		overlay?.addEventListener('click', closeModal);

		// Close on ESC key
		document.addEventListener('keydown', (e) => {
			if (e.key === 'Escape' && modal?.classList.contains('active')) {
				closeModal();
			}
		});
	});
</script>

