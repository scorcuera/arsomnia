---
import Layout from '../../layouts/Layout.astro';
import { useTranslations } from '../../i18n/utils';

const t = useTranslations('en');
---

<Layout title="Énergie Sync - arsomnia" lang="en">
	<!-- Hero Section -->
	<section class="project-hero">
		<div class="container">
			<a href="/#proyectos" class="back-link">
				<svg width="20" height="20" viewBox="0 0 20 20" fill="none">
					<path d="M15 10H5M5 10L10 15M5 10L10 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
				</svg>
				Back to Projects
			</a>
			<div class="hero-content">
				<h1 class="project-title">Énergie Sync</h1>
				<p class="project-tagline">Automated billing reconciliation for energy providers</p>
			</div>
		</div>
	</section>

	<!-- Hero Image -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-hero">
				<img src="/dashboard-energie-hero.png" alt="Real-time anomaly dashboard" />
				<p class="image-caption">Real-time anomaly dashboard showing reconciliation status across 40+ clients</p>
			</div>
		</div>
	</section>

	<!-- The Problem -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">The Problem</h2>
			<p class="section-text">
				Energy companies manage consumption data, invoices, and payments across disconnected systems. 
				Reconciling these sources manually takes hours every cycle—and errors still slip through.
			</p>
			<p class="section-text">
				Finance teams spend more time chasing discrepancies than analyzing trends.
			</p>
		</div>
	</section>

	<!-- The Solution -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">The Solution</h2>
			<p class="section-text">
				An automated pipeline that consolidates three data sources, cross-references every transaction, 
				and surfaces anomalies before they become problems.
			</p>
		</div>
	</section>

	<!-- How It Works -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">How It Works</h2>
			
			<div class="workflow-step">
				<h3 class="step-title">1. Data Ingestion</h3>
				<p class="section-text">
					Consumption records, invoices, and bank payments are pulled automatically from their respective systems.
				</p>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">2. Reconciliation Engine</h3>
				<p class="section-text">
					A custom n8n workflow merges records by client and period, then runs validation rules against the consolidated dataset.
				</p>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">3. Anomaly Detection</h3>
				<p class="section-text">The system flags three types of issues:</p>
				<ul class="feature-list">
					<li>Consumption without invoice (orphan records)</li>
					<li>Payment-invoice mismatches (amount discrepancies)</li>
					<li>Overdue invoices without payment</li>
				</ul>
			</div>

			<div class="workflow-step">
				<h3 class="step-title">4. Operational Dashboard</h3>
				<p class="section-text">
					Results flow into an Airtable Interface where the operations team can triage, assign, and resolve each case.
				</p>
			</div>
		</div>
	</section>

	<!-- Workflow Screenshot -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-workflow">
				<img src="/n8n-workflow-energie.png" alt="n8n workflow" />
				<p class="image-caption">The n8n workflow handling data merge and anomaly detection logic</p>
			</div>
		</div>
	</section>

	<!-- Stack -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Stack</h2>
			<div class="stack-table">
				<div class="stack-row">
					<div class="stack-tool">n8n</div>
					<div class="stack-role">Orchestration & business logic</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">Airtable</div>
					<div class="stack-role">Data storage & dashboard</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">Google Sheets</div>
					<div class="stack-role">Consumption data source</div>
				</div>
				<div class="stack-row">
					<div class="stack-tool">REST API</div>
					<div class="stack-role">Bank payment integration</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Results -->
	<section class="project-section">
		<div class="container-narrow">
			<h2 class="section-title">Results</h2>
			<div class="results-grid">
				<div class="result-item">
					<div class="result-number">3</div>
					<div class="result-text">data sources consolidated automatically</div>
				</div>
				<div class="result-item">
					<div class="result-number">12</div>
					<div class="result-text">anomalies detected per cycle</div>
				</div>
				<div class="result-item">
					<div class="result-number">~2h</div>
					<div class="result-text">saved per reconciliation</div>
				</div>
				<div class="result-item">
					<div class="result-number">0</div>
					<div class="result-text">manual data entry</div>
				</div>
			</div>
		</div>
	</section>

	<!-- Dashboard Detail -->
	<section class="project-section">
		<div class="container">
			<div class="project-image-centered">
				<img src="/dashboard-energie-detail-1.png" alt="Dashboard detail" />
				<p class="image-caption">Anomaly breakdown by type and severity, with actionable next steps</p>
			</div>
		</div>
	</section>

	<!-- CTA Section -->
	<section class="project-section project-cta">
		<div class="container-narrow">
			<div class="cta-buttons">
				<a href="/#contacto" class="btn btn-primary">Get in Touch</a>
				<a href="/#proyectos" class="btn btn-secondary">View More Projects</a>
			</div>
		</div>
	</section>

	<!-- Footer -->
	<footer class="footer">
		<div class="container">
			<div class="footer-content">
				<p class="footer-text">© 2025 arsomnia</p>
			</div>
		</div>
	</footer>
</Layout>

<style>
	/* Project Hero */
	.project-hero {
		padding: 8rem 0 4rem;
		background: var(--color-bg);
	}

	.back-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		color: var(--color-text-light);
		text-decoration: none;
		font-size: 0.95rem;
		margin-bottom: 3rem;
		margin-left: 5rem; /* Separación del borde */
		transition: all 0.3s ease;
		border: 1px solid rgba(212, 175, 55, 0.2);
		padding: 0.6rem 1.2rem;
		border-radius: 0;
	}

	.back-link:hover {
		color: var(--color-accent);
		border-color: var(--color-accent);
		background: rgba(212, 175, 55, 0.05);
	}

	.hero-content {
		text-align: center;
		max-width: 900px;
		margin: 0 auto;
	}

	.project-title {
		font-size: 3.5rem;
		font-weight: 700;
		margin-bottom: 1rem;
		color: var(--color-text);
	}

	.project-tagline {
		font-size: 1.3rem;
		color: var(--color-text-light);
		font-weight: 300;
	}

	/* Sections */
	.project-section {
		padding: 4rem 0;
	}

	.project-cta {
		padding: 6rem 0;
	}

	.container-narrow {
		max-width: 800px;
		margin: 0 auto;
		padding: 0 2rem;
	}

	.section-title {
		font-size: 2.5rem;
		font-weight: 700;
		margin-bottom: 2rem;
		color: var(--color-text);
	}

	.section-text {
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--color-text-light);
		margin-bottom: 1.5rem;
	}

	/* Images */
	.project-image-hero {
		max-width: 1100px;
		margin: 0 auto;
	}

	.project-image-workflow {
		max-width: 1100px;
		margin: 0 auto;
	}

	.project-image-centered {
		max-width: 1000px;
		margin: 0 auto;
	}

	.project-image-hero img,
	.project-image-workflow img,
	.project-image-centered img {
		width: 100%;
		height: auto;
		display: block;
		border: 1px solid rgba(212, 175, 55, 0.2);
	}

	.image-caption {
		text-align: center;
		font-size: 0.9rem;
		color: var(--color-text-light);
		margin-top: 1rem;
		font-style: italic;
	}

	/* Workflow Steps */
	.workflow-step {
		margin-bottom: 3rem;
	}

	.step-title {
		font-size: 1.5rem;
		font-weight: 600;
		color: var(--color-accent);
		margin-bottom: 1rem;
	}

	.feature-list {
		list-style: none;
		padding: 0;
		margin: 1.5rem 0;
	}

	.feature-list li {
		padding-left: 1.5rem;
		position: relative;
		margin-bottom: 0.75rem;
		color: var(--color-text-light);
		font-size: 1.1rem;
	}

	.feature-list li::before {
		content: '→';
		position: absolute;
		left: 0;
		color: var(--color-accent);
	}

	/* Stack Table */
	.stack-table {
		border: 1px solid rgba(212, 175, 55, 0.2);
	}

	.stack-row {
		display: grid;
		grid-template-columns: 200px 1fr;
		border-bottom: 1px solid rgba(212, 175, 55, 0.1);
	}

	.stack-row:last-child {
		border-bottom: none;
	}

	.stack-tool {
		padding: 1.25rem 1.5rem;
		font-weight: 600;
		color: var(--color-accent);
		border-right: 1px solid rgba(212, 175, 55, 0.1);
	}

	.stack-role {
		padding: 1.25rem 1.5rem;
		color: var(--color-text-light);
	}

	/* Results Grid */
	.results-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 2rem;
		margin-top: 2rem;
	}

	.result-item {
		text-align: center;
		padding: 2rem;
		border: 1px solid rgba(212, 175, 55, 0.2);
		background: rgba(13, 13, 13, 0.3);
	}

	.result-number {
		font-size: 3rem;
		font-weight: 700;
		color: var(--color-accent);
		margin-bottom: 0.5rem;
	}

	.result-text {
		font-size: 1rem;
		color: var(--color-text-light);
	}

	/* CTA Buttons */
	.cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.btn {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 1rem 2rem;
		font-size: 1rem;
		font-weight: 500;
		text-decoration: none;
		border-radius: 0;
		transition: all 0.3s ease;
		font-family: var(--font-main);
		border: 2px solid;
	}

	.btn-primary {
		background: var(--color-accent);
		color: var(--color-bg);
		border-color: var(--color-accent);
	}

	.btn-primary:hover {
		background: transparent;
		color: var(--color-accent);
	}

	.btn-secondary {
		background: transparent;
		color: var(--color-text);
		border-color: rgba(212, 175, 55, 0.3);
	}

	.btn-secondary:hover {
		border-color: var(--color-accent);
		color: var(--color-accent);
	}

	/* Footer */
	.footer {
		padding: 3rem 0;
		border-top: 1px solid rgba(212, 175, 55, 0.2);
	}

	.footer-content {
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.footer-text {
		color: var(--color-text-light);
		font-size: 0.95rem;
	}

	/* Responsive */
	@media (max-width: 768px) {
		.project-hero {
			padding: 6rem 0 3rem;
		}

		.back-link {
			padding: 0.5rem 1rem;
			font-size: 0.9rem;
			margin-left: 1rem;
		}

		.project-title {
			font-size: 2.5rem;
		}

		.project-tagline {
			font-size: 1.1rem;
		}

		.section-title {
			font-size: 2rem;
		}

		.section-text {
			font-size: 1rem;
		}

		.stack-row {
			grid-template-columns: 1fr;
		}

		.stack-tool {
			border-right: none;
			border-bottom: 1px solid rgba(212, 175, 55, 0.1);
		}

		.results-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.result-number {
			font-size: 2.5rem;
		}

		.cta-buttons {
			flex-direction: column;
		}

		.btn {
			width: 100%;
			justify-content: center;
		}
	}
</style>

